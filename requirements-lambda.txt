# Production-only dependencies for AWS Lambda.
# Excluded vs requirements.txt:
#   uvicorn          -> not needed (Mangum is the server adapter)
#   psycopg2-binary  -> only asyncpg is used at runtime
#   alembic          -> migrations run locally, not inside Lambda
#   boto3            -> already bundled in the Lambda Python runtime
#   python-magic     -> unused in the codebase (and needs libmagic)
#   pytest/*         -> dev/test only

# FastAPI + Lambda adapter
fastapi==0.115.6
mangum==0.19.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# Validation & Settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.18

# OAuth
authlib==1.3.2
itsdangerous==2.2.0

# HTTP Client (for Spotify API)
httpx==0.28.1

# Redis (cache - fails gracefully if unavailable)
redis==5.2.1

# AI Chatbot
google-generativeai==0.8.3

# Email
resend==2.19.0

# Image processing
pillow==11.0.0
